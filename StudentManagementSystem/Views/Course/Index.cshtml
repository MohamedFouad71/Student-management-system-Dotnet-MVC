@* ########################################################## *@
@* ################ this is the course index ################ *@
@* ########################################################## *@

@model List<Course>

<link rel="stylesheet" href="~/css/views.css" />

<h2 class="text-center my-4">Courses</h2>

<div class="d-flex justify-content-end mb-3">
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> New Course
    </a>
</div>

<table class="table table-bordered table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th scope="col">Course ID</th>
            <th scope="col">Course Name</th>
            <th scope="col">Credit Hours</th>
            <th scope="col">Department</th>
            <th scope="col">Delete</th>
            <th scope="col">Modify</th>
            <th scope="col">Attendance</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model)
        {
            <tr>
                <td>@course.Id</td>
                <td>@course.Name</td>
                <td>@course.Credits</td>
                <td>@course.Department.Name</td>
                <td>
                    <form asp-action="CourseDelete" method="post" class="d-inline">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@course.Id" />
                        <button type="submit" class="btn btn-danger btn-sm"
                                onclick="return confirm('Are you sure you want to delete this course?');">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </form>
                </td>
                <td>
                    <a asp-action="Modify" asp-route-id="@course.Id" class="btn btn-warning btn-sm">Modify</a>
                </td>
                <td>
                    <a asp-controller="Attendance" asp-action="CourseAttendance" asp-route-id="@course.Id" class="btn btn-sm btn-info">
                        Attendance
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
